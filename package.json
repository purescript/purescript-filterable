{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test",
    "build:docs": "rimraf docs && pulp docs"
  },
  "devDependencies": {
    "pulp": "^10.0.1",
    "purescript-psa": "^0.4.0",
    "rimraf": "^2.6.0"
  }
}
